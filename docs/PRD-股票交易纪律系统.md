# 个人股票交易纪律系统 - 产品需求文档 (PRD)

**版本**: v2.0
**日期**: 2026-03-24
**作者**: 全栈工程师

---

## 一、项目概述

### 1.1 项目背景
基于您已有的《个人股票交易纪律系统规则》，将纸质规则系统化、辅助化，开发一个智能股票交易纪律辅助系统。

### 1.2 系统定位（重要澄清）
**本系统定位：仅做辅助决策，不进行实际交易**

| 功能范围 | 说明 |
|----------|------|
| ✅ AI选股 | 根据规则筛选符合条件的股票标的 |
| ✅ K线图展示 | 专业K线图+技术指标+买卖点标注 |
| ✅ 买卖点建议 | 基于规则的买入/卖出信号提示 |
| ✅ 纪律执行提醒 | 止损止盈/风控规则触发提醒 |
| ❌ 实际交易 | 不对接券商下单接口，不执行实际买卖 |

### 1.3 项目目标
- **AI选股自动化**: 根据规则自动筛选符合条件的股票标的
- **K线图可视化**: 专业K线图展示个股技术形态
- **买卖提示智能化**: 基于规则引擎生成买入/卖出信号建议
- **纪律执行辅助**: 止损止盈/风控规则触发时提供人工确认提醒

### 1.4 核心价值
| 价值点 | 描述 |
|--------|------|
| 纪律执行 | 克服人性弱点，严格执行交易规则 |
| 效率提升 | 自动筛选+人工确认，减少盯盘时间 |
| 风险控制 | 8%硬止损+阶梯止盈提示，守住本金 |
| 数据驱动 | 量化选股条件，减少主观判断 |
| 人工决策 | 所有买卖决策由人工完成，系统仅提供参考 |

---

## 二、功能需求详述

### 2.1 模块一：AI选股模块

#### 2.1.1 选股规则管理

**系统默认规则（内置）**

| 规则类型 | 规则名称 | 量化参数 |
|----------|----------|----------|
| 排除规则 | ST/\*ST股排除 | 退市风险警示股 |
| 排除规则 | 市值过滤 | 流通市值20亿~500亿 |
| 排除规则 | 利空排除 | 近3交易日重大利空 |
| 排除规则 | 僵尸股排除 | 近20日无涨停/无放量 |
| 基础门槛 | 资金异动 | 20日内1次涨停或3日换手率≥2倍 |
| 核心标的 | 放量长上影 | 上影线≥50%振幅，量≥2倍均量 |
| 核心标的 | 一进二接力 | 首板后第1交易日，市值≤100亿 |
| 核心标的 | 抗跌强势股 | 大盘跌时个股拒绝跟跌 |

**自定义规则（用户可配置）**
- 支持新增自定义选股条件
- 支持修改量化参数阈值
- 支持启用/禁用特定规则
- 支持规则分组管理

#### 2.1.2 实时股票数据对接

**数据源架构**

```
┌─────────────────────────────────────────┐
│           数据源层 (Data Source)         │
├─────────────────────────────────────────┤
│  AKShare (主) │ Tushare (备) │ 东方财富  │
│  免费开源      │ Pro版付费    │ 实时数据  │
└─────────────────────────────────────────┘
```

**数据字段清单**

| 数据类型 | 字段 | 说明 |
|----------|------|------|
| 基础行情 | code, name, open, high, low, close, volume | 股票代码/名称/开高低收/成交量 |
| 实时数据 | price, change, change_pct, amplitude | 当前价/涨跌额/涨跌幅/振幅 |
| 资金流向 | main_net_inflow, main_net_rate | 主力净流入/净流入率 |
| 市场统计 | up_count, down_count, total_count | 上涨/下跌/总股票数 |
| 历史数据 | date, open, high, low, close, volume, turnover | 日K线完整数据 |

**API接口设计**

```python
# 选股服务API
POST /api/stock/screening
{
  "rules": ["规则ID列表"],
  "market": "all" | "sh" | "sz",
  "sort_by": "volume" | "change_pct" | "net_inflow",
  "limit": 20
}

# K线数据API
GET /api/stock/kline/{code}?period=daily&count=60

# 实时行情API
GET /api/stock/realtime/{code}

# 市场概览API
GET /api/market/overview
```

#### 2.1.3 选股流程设计

```
┌──────────────┐
│  获取全市场   │
│  股票列表     │
└──────┬───────┘
       ↓
┌──────────────┐
│  应用排除规则 │ ←── ST股/市值/利空
│  (并行过滤)   │
└──────┬───────┘
       ↓
┌──────────────┐
│  应用核心规则 │ ←── 长上影/一进二/抗跌
│  (量化评分)   │
└──────┬───────┘
       ↓
┌──────────────┐
│  排序输出     │
│  Top 10      │
└──────────────┘
```

---

### 2.2 模块二：K线图展示模块

#### 2.2.1 K线图功能（使用klinecharts）

**为什么选择klinecharts？**

| 特性 | klinecharts | ECharts | 对比 |
|------|-------------|---------|------|
| 定位 | 专业金融K线图表 | 通用图表库 | klinecharts更专业 |
| 体积 | ~50KB Gzip | ~300KB Gzip | klinecharts更轻量 |
| K线性能 | 专门优化 | 通用渲染 | klinecharts更流畅 |
| 内置指标 | MA/MACD/RSI等内置 | 需手动配置 | klinecharts更便捷 |
| 分时图 | 原生支持 | 需插件 | klinecharts更完善 |
| 标注功能 | 原生支持买卖点标注 | 需自定义 | klinecharts更适合 |

**图表类型**
| 图表类型 | 说明 |
|----------|------|
| 日K线 | 默认显示60个交易日 |
| 周K线 | 显示20周 |
| 分时图 | 当日分时走势 |
| 对比图 | 多股叠加对比 |

**技术指标叠加**
- 均价线 (MA5, MA10, MA20, MA60)
- 成交量柱状图
- 涨跌幅标注
- MACD / RSI（可选）

**交互功能**
- 缩放/平移
- K线hover显示OHLCV详情
- 点击K线显示当日买卖提示状态

#### 2.2.2 买卖提示标注

**买入信号标注**

| 信号类型 | 标注样式 | 出现位置 |
|----------|----------|----------|
| 缩量企稳买入 | 🟢 绿色圆点 | 均价线支撑处 |
| 弱转强买入 | 🔵 蓝色三角 | 上穿均价线时 |
| 尾盘确定性买入 | 🟡 黄色方块 | 14:40后 |
| 打板买入 | 🔴 红色箭头 | 涨停价封板瞬间 |

**卖出信号标注**

| 信号类型 | 标注样式 | 出现位置 |
|----------|----------|----------|
| 止损提醒 | ⚠️ 红色警报 | 跌破关键支撑价 |
| 止盈提醒 | 💰 绿色提示 | 达到阶梯止盈点 |
| 破位卖出 | ⛔ 黑色叉号 | 触发破位规则 |

#### 2.2.3 界面布局设计

```
┌─────────────────────────────────────────────────────────────┐
│  头部: 股票搜索 | 自选股列表 | 市场涨跌家数 | 账户概览        │
├─────────────────────────────────────────────────────────────┤
│                    │                                        │
│    K线图主区域      │         买卖信号面板                    │
│    (klinecharts)  │         - 当前持仓                     │
│                    │         - 买入条件确认                 │
│                    │         - 卖出信号提醒                 │
│                    │                                        │
├─────────────────────────────────────────────────────────────┤
│  底部: 规则引擎状态 | 数据更新 | 最后更新时间                 │
└─────────────────────────────────────────────────────────────┘
```

---

### 2.3 模块三：规则引擎模块

#### 2.3.1 买入规则引擎

**前置条件检查**
```
IF 9:40上涨家数 < 3000:
    → 禁止开仓提醒
ELIF 9:40上涨家数 >= 3500:
    → 最高仓位80%，最多6只
ELIF 9:40上涨家数 >= 3000:
    → 最高仓位50%，最多5只
```

**买入形态识别**

| 形态 | 识别条件 | 建议时机 |
|------|----------|----------|
| 缩量企稳 | 回调成交量<开盘1分钟量30%，持续≥5分钟 | 企稳时建议 |
| 弱转强 | 开盘放量跌→上穿均价线→缩量回踩 | 回踩均价线时建议 |
| 尾盘确定 | 14:40后缩量横盘+不破均价线 | 尾盘窗口建议 |
| 打板 | 涨停价封板瞬间 | 封板瞬间建议 |

#### 2.3.2 卖出规则引擎

**执行优先级**
```
1. 止损铁律 (最高优先级)
   - 逻辑破位 → 止损提醒
   - 浮亏≥8% → 无条件止损提醒

2. 止盈规则
   - 浮盈≥3% → 跌破均价线卖50%提醒
   - 浮盈≥5% → 跌破开盘价卖全部提醒
   - 浮盈≥10% → 卖50%+移动止盈提醒

3. 破位卖出
   - 2次上冲不破均价线
   - 放量滞涨
   - 三根K线逐级降低
```

#### 2.3.3 风控规则引擎

**账户风控**
```
连续2日亏损 → 仓位降至30%提醒
连续3日亏损 → 全仓清仓+暂停1日提醒
单周回撤≥10% → 全仓清仓+暂停3日提醒
单日回撤≥5% → 次日禁止新开仓提醒
```

**大盘极端行情**
```
9:40下跌家数≥3000 → 减半仓提醒
9:40下跌家数≥4000 → 全仓清仓提醒
开盘10分钟跌幅≥2% → 全仓清仓提醒
```

---

### 2.4 模块四：用户管理模块

#### 2.4.1 自选股管理
- 添加/删除自选股
- 自选股分类（观察池/买入池/持仓/卖出）
- 自选股备注信息

#### 2.4.2 持仓管理（仅记录人工操作）
- 持仓列表展示（需手动录入成本）
- 持仓成本/盈亏显示
- 持仓个股规则信号状态

#### 2.4.3 交易记录（仅记录，不执行）
- 买入记录（时间/价格/数量/理由）
- 卖出记录（时间/价格/数量/盈亏）
- 违规记录标记

#### 2.4.4 规则配置管理
- 查看系统内置规则
- 创建/编辑自定义规则
- 启用/禁用规则
- 规则参数调整

---

## 三、技术架构设计

### 3.1 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      前端展示层 (Vue 3)                      │
│   K线图(klinecharts) │ 选股列表 │ 买卖信号 │ 持仓管理 │ 规则配置 │
├─────────────────────────────────────────────────────────────┤
│                      API网关层 (FastAPI)                     │
│   REST API │ WebSocket (实时数据) │ 规则引擎接口              │
├─────────────────────────────────────────────────────────────┤
│                      业务逻辑层 (Python)                      │
│   选股服务 │ 规则引擎 │ K线生成 │ 风控计算 │ 用户服务         │
├─────────────────────────────────────────────────────────────┤
│                      数据层 (SQLite + Redis)                 │
│   股票数据 │ 用户数据 │ 缓存层                               │
├─────────────────────────────────────────────────────────────┤
│                      数据源层 (AKShare / Tushare)             │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 技术栈选型（优化版）

| 层级 | 技术选型 | 说明 | 选型理由 |
|------|----------|------|----------|
| 前端框架 | Vue 3 + TypeScript | 渐进式框架 | 轻量易学，与您偏好一致 |
| UI组件库 | Element Plus | Vue3官方组件库 | 企业级组件，风格统一 |
| 图表库 | klinecharts | 专业金融K线 | 轻量(~50KB)、专业、标注原生支持 |
| 状态管理 | Pinia | Vue3官方推荐 | 轻量、TypeScript友好 |
| 构建工具 | Vite |下一代构建工具| 快速HMR，开发体验好 |
| 后端框架 | FastAPI | 高性能Python框架 | 自动文档，类型安全 |
| 数据处理 | Pandas + NumPy | 数据分析 | 量化规则计算必备 |
| 选股引擎 | 自研量化引擎 | 基于规则 | 完全可控，贴合需求 |
| 数据库 | SQLite (开发) / PostgreSQL (生产) | 关系型 | 轻量/生产级 |
| 缓存 | Redis | 实时数据缓存 | 高性能 |
| 定时任务 | APScheduler | 定时拉取数据 | 轻量易用 |
| 数据源 | AKShare (主) + Tushare (备) | 免费开源 | 数据全面，更新及时 |

### 3.3 为什么选择Vue而非React？

| 对比项 | Vue 3 | React 18 | 结论 |
|--------|-------|----------|------|
| 学习曲线 | 平缓 | 较陡 | Vue更适合快速开发 |
| TypeScript支持 | 原生TS支持 | 需额外配置 | Vue更友好 |
| 状态管理 | Pinia(轻量) | Redux(复杂) | Vue更简洁 |
| 生态定位 | 渐进式框架 | 视图库 | Vue更灵活 |
| 组件写法 | Options/Composition | Hooks | Composition与React Hooks类似 |
| 国内生态 | 较广 | 广 | 两者相当 |

**结论**：两者皆可完成本项目，Vue 3 + Element Plus组合更适合快速迭代。

### 3.4 为什么选择klinecharts而非ECharts？

| 对比项 | klinecharts | ECharts | 结论 |
|--------|-------------|---------|------|
| 定位 | 专业金融K线 | 通用可视化 | klinecharts更专业 |
| K线性能 | 专为K线优化 | 通用渲染 | 大数据量下klinecharts更流畅 |
| 体积 | ~50KB Gzip | ~300KB Gzip | klinecharts轻量80% |
| 分时图 | 原生支持 | 需组合多个系列 | klinecharts更简单 |
| 标注功能 | 原生买卖点标注API | 需自定义实现 | klinecharts开箱即用 |
| 内置指标 | MA/MACD/RSI等 | 需手动组合 | klinecharts更全面 |
| 配置复杂度 | 5行代码入门 | 配置项繁多 | klinecharts更易用 |

**结论**：klinecharts是专为股票K线设计的库，比ECharts更适合本项目。

### 3.5 数据库设计

```sql
-- 股票基础信息表
CREATE TABLE stocks (
    code VARCHAR(10) PRIMARY KEY,
    name VARCHAR(50),
    market VARCHAR(10),  -- sh/sz/bj
    list_date DATE,
    created_at TIMESTAMP
);

-- 日K线数据表
CREATE TABLE daily_kline (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    code VARCHAR(10),
    trade_date DATE,
    open DECIMAL(10,2),
    high DECIMAL(10,2),
    low DECIMAL(10,2),
    close DECIMAL(10,2),
    volume BIGINT,
    turnover DECIMAL(10,2),
    UNIQUE(code, trade_date)
);

-- 自选股表
CREATE TABLE watchlist (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    stock_code VARCHAR(10),
    category VARCHAR(20),  -- observe/buy/pool/held/sold
    note TEXT,
    added_at TIMESTAMP
);

-- 持仓记录表（人工录入，不自动交易）
CREATE TABLE positions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    stock_code VARCHAR(10),
    buy_date DATE,
    buy_price DECIMAL(10,2),     -- 成本价（人工录入）
    buy_quantity INTEGER,         -- 持仓数量（人工录入）
    buy_reason VARCHAR(200),      -- 买入理由
    stop_loss DECIMAL(10,2),      -- 止损价（系统计算）
    target_price DECIMAL(10,2),   -- 目标价（系统计算）
    status VARCHAR(20),  -- open/closed
    closed_at TIMESTAMP,
    created_at TIMESTAMP
);

-- 交易记录表（人工记录，不执行交易）
CREATE TABLE trades (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    position_id INTEGER,
    stock_code VARCHAR(10),
    trade_date DATE,
    action VARCHAR(10),  -- buy/sell
    price DECIMAL(10,2),
    quantity INTEGER,
    profit DECIMAL(10,2),
    rule_triggered VARCHAR(50),
    is_violation BOOLEAN DEFAULT FALSE,
    note TEXT,
    created_at TIMESTAMP
);

-- 选股规则表
CREATE TABLE stock_rules (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(100),
    type VARCHAR(20),  -- exclude/core/custom
    category VARCHAR(20),  -- long_shadow/one_to_two/resilient
    config JSON,  -- 规则配置
    enabled BOOLEAN DEFAULT TRUE,
    is_system BOOLEAN DEFAULT FALSE,
    user_id INTEGER,
    priority INTEGER DEFAULT 0,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- 市场数据缓存表
CREATE TABLE market_cache (
    key VARCHAR(50) PRIMARY KEY,
    value JSON,
    updated_at TIMESTAMP
);

-- 用户配置表
CREATE TABLE user_config (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    config_key VARCHAR(50),
    config_value JSON,
    updated_at TIMESTAMP
);
```

---

## 四、数据API接口设计

### 4.1 选股API

```yaml
POST /api/v1/stocks/screen
Summary: 选股筛选接口
Request:
  {
    "rule_ids": [1, 2, 3],        # 指定规则ID列表
    "market": "all",               # 市场筛选
    "sort_by": "change_pct",       # 排序字段
    "sort_order": "desc",
    "limit": 20
  }
Response:
  {
    "code": 0,
    "data": {
      "total": 150,
      "stocks": [
        {
          "code": "000001",
          "name": "平安银行",
          "price": 12.50,
          "change_pct": 5.23,
          "volume": 50000000,
          "signals": ["放量长上影", "抗跌强势"],
          "score": 85,
          "reason": "上影线占比65%，量能2.5倍均量"
        }
      ]
    }
  }
```

### 4.2 K线API

```yaml
GET /api/v1/stocks/{code}/kline
Summary: 获取K线数据
Query:
  - period: daily|weekly|monthly
  - start_date: YYYY-MM-DD
  - end_date: YYYY-MM-DD
  - count: 数量

GET /api/v1/stocks/{code}/realtime
Summary: 获取实时行情
Response:
  {
    "code": "000001",
    "name": "平安银行",
    "price": 12.50,
    "open": 12.00,
    "high": 13.00,
    "low": 11.80,
    "volume": 50000000,
    "bid1": 12.50,
    "ask1": 12.51,
    "main_net_inflow": 1000000
  }
```

### 4.3 规则信号API

```yaml
GET /api/v1/stocks/{code}/signals
Summary: 获取个股买卖信号
Response:
  {
    "code": "000001",
    "signals": {
      "buy": [
        {
          "type": "缩量企稳",
          "price": 12.30,
          "confidence": 0.85,
          "triggered_at": "09:45:00",
          "reason": "回调缩量至25%，均价线支撑有效"
        }
      ],
      "sell": [
        {
          "type": "止损提醒",
          "price": 11.50,
          "reason": "跌破支撑位（长上影最低价）",
          "priority": "HIGH",
          "current_loss": "8.3%"
        }
      ]
    }
  }

GET /api/v1/market/signals
Summary: 获取市场整体信号
Response:
  {
    "up_count": 3200,
    "down_count": 1800,
    "can_open": true,           # 是否可以开仓
    "max_position": "50%",        # 最高仓位
    "risk_level": "MEDIUM",       # 风险等级
    "suggestion": "谨慎开仓"
  }
```

---

## 五、界面设计原型

### 5.1 主页布局

```
┌──────────────────────────────────────────────────────────────────┐
│  [Logo] 个人股票交易纪律系统    [市场]↑3200 ↓1800  09:40 可开仓 50% │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                    K 线 图 区 域                              │ │
│  │  ┌─────────────────────────────────────────┐                │ │
│  │  │ 📈 000001 平安银行  12.50 (+5.23%)       │                │ │
│  │  ├─────────────────────────────────────────┤                │ │
│  │  │                                          │                │ │
│  │  │     klinecharts 专业K线图               │  ┌───────────┐ │ │
│  │  │     MA5/MA10/MA20叠加                   │  │ 买卖信号   │ │ │
│  │  │     成交量柱状图                        │  │ 🟢买入信号 │ │ │
│  │  │     买卖点标注                          │  │ ⚠️止损提醒 │ │ │
│  │  │                                        │  │ 💰止盈提醒 │ │ │
│  │  │  [买入标注]           [卖出标注]        │  └───────────┘ │ │
│  │  └─────────────────────────────────────────┘                │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  ┌───────────────────────┐  ┌────────────────────────────────┐ │
│  │      今日选股池        │  │         持仓管理                  │ │
│  │  ┌──────────────────┐ │  │  ┌────────────────────────────┐ │ │
│  │  │ 1. 000002 万科A  │ │  │  │ 持仓  股票   成本   盈亏    │ │ │
│  │  │    放量长上影 85分│ │  │  │ 1.   000001  12.0  +4.2%  │ │ │
│  │  │ 2. 600519 茅台   │ │  │  │ 2.   600519  1800 +2.1%  │ │ │
│  │  │    一进二  78分  │ │  │  └────────────────────────────┘ │ │
│  │  │ 3. 000858 五粮液 │ │  │  [录入持仓] [编辑成本]           │ │
│  │  │    抗跌强势  72分 │ │  │                                 │ │
│  │  └──────────────────┘ │  │                                 │ │
│  └───────────────────────┘  └────────────────────────────────┘ │
│                                                                   │
├──────────────────────────────────────────────────────────────────┤
│  系统状态: ✅数据连接正常  🕐最后更新: 09:41:23  📊今日选股: 15只   │
│  ⚠️ 纪律提醒: 持仓000001触及8%止损线，请人工确认是否执行止损        │
└──────────────────────────────────────────────────────────────────┘
```

### 5.2 买入提示弹窗

```
┌─────────────────────────────────────────────────────┐
│  🟢 买入信号建议                                      │
├─────────────────────────────────────────────────────┤
│                                                      │
│  股票: 000001 平安银行                               │
│  当前价: 12.50 (+5.23%)                             │
│  信号类型: 缩量企稳买入                              │
│  建议买入区间: 12.30-12.50                          │
│  建议仓位: 10% (当日最高50%)                        │
│                                                      │
│  ┌──────────────────────────────────────────────┐   │
│  │ 规则匹配分析                                    │   │
│  │ ✅ 9:40上涨家数3200,满足开仓条件               │   │
│  │ ✅ 缩量企稳形态确认（回调量25%）               │   │
│  │ ✅ 回踩均价线12.30成功                         │   │
│  │ ✅ 连续5分钟企稳                               │   │
│  │ ⚠️ 注意: 处于横盘整理阶段                      │   │
│  └──────────────────────────────────────────────┘   │
│                                                      │
│  ┌──────────────────────────────────────────────┐   │
│  │ 💡 人工决策提示                                 │   │
│  │ 本系统仅提供参考，买卖决策由您人工完成          │   │
│  └──────────────────────────────────────────────┘   │
│                                                      │
│  [关闭]                                              │
│                                                      │
└─────────────────────────────────────────────────────┘
```

### 5.3 卖出提醒弹窗

```
┌─────────────────────────────────────────────────────┐
│  ⚠️ 止损/止盈提醒                                    │
├─────────────────────────────────────────────────────┤
│                                                      │
│  持仓: 000001 平安银行                              │
│  成本价: 12.00  当前价: 11.04 (-8.0%)              │
│  持仓盈亏: -400元 (-6.67%)                          │
│                                                      │
│  ┌──────────────────────────────────────────────┐   │
│  │ 触发规则: 绝对亏损止损 (8%)                    │   │
│  │ 建议动作: 全仓止损                             │   │
│  │ 执行时间: 人工确认后执行                       │   │
│  └──────────────────────────────────────────────┘   │
│                                                      │
│  ┌──────────────────────────────────────────────┐   │
│  │ ⚠️ 重要提示                                     │   │
│  │ 根据交易纪律规则，触发止损条件时应严格执行       │   │
│  │ 本系统仅提供提醒，买卖决策由您人工完成           │   │
│  └──────────────────────────────────────────────┘   │
│                                                      │
│  [关闭]          [我已知晓，人工执行卖出]            │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## 六、开发里程碑

### Phase 1: 基础架构搭建 (第1-2周)
| 任务 | 交付物 |
|------|--------|
| 项目初始化 (Vue3 + FastAPI) | Git仓库/基础架构 |
| 数据层搭建 | SQLite/数据模型 |
| AKShare集成 | 股票数据拉取服务 |
| klinecharts集成 | K线图基础组件 |

### Phase 2: 选股模块 (第3-4周)
| 任务 | 交付物 |
|------|--------|
| 规则引擎开发 | 规则解析/执行引擎 |
| 排除规则实现 | ST股市值过滤 |
| 核心规则实现 | 三类标的筛选 |
| 选股API | 选股接口 |

### Phase 3: K线与信号模块 (第5-6周)
| 任务 | 交付物 |
|------|--------|
| K线数据存储 | 日K线数据库 |
| klinecharts配置 | 专业K线组件 |
| 买卖信号识别 | 信号规则引擎 |
| 信号标注展示 | 买卖点标注 |

### Phase 4: 风控与提醒 (第7-8周)
| 任务 | 交付物 |
|------|--------|
| 风控规则实现 | 账户/大盘风控 |
| 止损止盈计算 | 条件价格计算 |
| 提醒推送 | 弹窗/声音提醒 |
| 持仓管理 | 人工录入/编辑 |

### Phase 5: 测试与优化 (第9-10周)
| 任务 | 交付物 |
|------|--------|
| 单元测试 | 测试覆盖率≥80% |
| 集成测试 | API测试 |
| 用户验收测试 | 完整流程测试 |
| 性能优化 | 响应时间<500ms |

---

## 七、待优化项与未来迭代

### 7.1 本次可优化项

| 优化点 | 优先级 | 说明 |
|--------|--------|------|
| 多数据源自动切换 | 高 | AKShare不稳定时自动切换Tushare |
| 本地数据缓存 | 高 | 减少API调用，提速 |
| 自定义规则UI | 中 | 可视化配置选股规则 |
| 历史回测 | 中 | 对规则效果进行历史验证 |
| 交易统计报表 | 中 | 胜率/盈亏统计 |

### 7.2 后续迭代计划

**v1.1 增强功能**
- 自定义规则可视化配置
- 历史回测功能
- 交易统计报表

**v1.2 高级功能**
- 微信/邮件推送提醒
- 多账号管理
- 策略组合管理

**v2.0 智能化（不涉及交易）**
- 机器学习辅助优化参数
- 自然语言规则输入
- 智能形态识别增强

---

## 八、风险与应对

| 风险项 | 影响程度 | 应对策略 |
|--------|----------|----------|
| 数据API不稳定 | 高 | 多数据源备份+本地缓存 |
| K线延迟 | 中 | 本地数据缓存+定时更新 |
| 规则量化误差 | 中 | 保留人工确认环节 |
| 系统崩溃 | 低 | 定时数据备份 |

---

## 九、附录

### 9.1 参考资料
- AKShare文档: https://akshare.akfamily.xyz/
- Tushare文档: https://tushare.pro/document
- klinecharts文档: https://klinecharts.com/
- Vue 3文档: https://vuejs.org/
- Element Plus文档: https://element-plus.org/

### 9.2 术语表
| 术语 | 说明 |
|------|------|
| 上影线 | 最高价与收盘/开盘价的差 |
| 缩量企稳 | 成交量萎缩+价格企稳 |
| 弱转强 | 开盘弱势后转强势上涨 |
| 移动止盈 | 跟随价格上涨提升止损点 |
| klinecharts | 专业金融K线图表库 |

---

**文档状态**: v2.0 草稿
**审批状态**: 待审批
**下一步**: 技术架构评审
